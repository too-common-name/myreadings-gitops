apiVersion: apps/v1
kind: Deployment
metadata:
  name: myreadings-ui
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: myreadings-ui
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
